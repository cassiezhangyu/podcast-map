# Changelog

## v1.3.0

- 增加跨平台转写运行时探测：复用已存在的 Python、虚拟环境、ASR 包和 Hugging Face 模型缓存，避免不同 Coding Agent 重复下载；
- macOS/Linux 检查 `.venv/bin/python`，Windows 检查 `.venv/Scripts/python.exe`；
- Windows 不假设支持 MLX，探测并报告已有的 `faster-whisper` 或 `openai-whisper` 后端；
- 按平台检查 `ffmpeg`、`ffprobe`、`afconvert` 和 `afinfo`，缺失时输出 `DEPENDENCY_BLOCKED` 或 `ALTERNATIVE_AVAILABLE`；
- 未经授权不自动安装依赖、下载模型或静默切换转写后端。

## v1.1.7

- 将总览升级为独立高密度旗舰页：同时呈现主关系、支撑系统、边界、反馈和代表性锚点，不用一条漂亮主线掩盖重要分支；
- 增加深读组图标准和逐页理解合同，要求每页给出不可替代的知识增量、主导关系及陌生读者入口；
- 固化封面的系列几何、灰色蒙版、官方节目图片、原标题、主持人、嘉宾和期数校验，并阻断封面与总览版本错配；
- 强化页面文字层级和容器排版：页面标题、分区标题、容器标题、正文和辅助信息依次降级，多行正文默认左对齐；
- 要求每条连接线都能复述“起点—关系—终点”，并在文字、字号、断行或节点变化后重新计算端点；
- 扩展 Excalidraw 机械审计，拦截标题正文同级、未经声明的多行正文居中、越界、重叠和线路压字；
- 共性缺陷必须回到共享组件、生成函数或通用规则修正，并回归整套选用页面，不把批注页码写成永久补丁；
- 新增页面理解、总览证明和封面几何模板，正式发布检查同步覆盖这些工件。

## v1.1.1

- 恢复 EP58 最终通过时的内容、页面和视觉决策基线；
- 将正式范围收敛为播客、长音频及其可靠转写稿；
- 增加来源章节与语义单元的双向覆盖要求；
- 禁止因受限环境报错而静默降级转写模型或覆盖正式转写；
- 保留陌生读者自然语言编辑和经过测试的 Excalidraw 越界审计；
- 新增 `skill-runtime.json`，阻断不同 skill 版本混用；
- 正式发布模式阻断缺失 `.excalidraw`、缺失关键工件和 `provisional` 页面；
- 当前失败的 E169 成品已作为反向回归样本，能够被新门禁拒绝。

## v1.1.0

- 加强陌生读者理解、自然语言、注意力密度和视觉检查规则。
- 此版本的发布目录与本机安装目录曾发生漂移，已由 v1.1.1 修复。
